import './ui-authenticate.module.scss';

/* eslint-disable-next-line */
export interface UiAuthenticateProps {}

export function UiAuthenticate(props: UiAuthenticateProps) {
  return (
    <div>
      <h1>Welcome to UiAuthenticate!</h1>
    </div>
  );
}

export default UiAuthenticate;
